apiVersion: v1
kind: Service
metadata:
  name: python-service-test
  namespace: default
  labels:
    app: python-service-test
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8000
    protocol: TCP
  selector:
    app: python-service-test