apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

commonLabels:
  app.kubernetes.io/part-of: diplom
  backstage.io/kubernetes-id: diplom

resources:
  - deployment.yaml