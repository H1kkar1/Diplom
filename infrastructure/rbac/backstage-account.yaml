# flux-backstage-sa.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backstage-reader
  namespace: flux-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: backstage-reader
rules:
# Доступ ко всем ресурсам во всех API группах - ТОЛЬКО ЧТЕНИЕ
- apiGroups: ["*"]
  resources: ["*"]
  verbs: ["get", "list", "watch"]
# Доступ к non-resource эндпоинтам
- nonResourceURLs: ["*"]
  verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: backstage-reader-binding
subjects:
- kind: ServiceAccount
  name: backstage-reader
  namespace: flux-system
roleRef:
  kind: ClusterRole
  name: backstage-reader
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: v1
kind: Secret
metadata:
  name: backstage-reader-token
  namespace: flux-system
  annotations:
    kubernetes.io/service-account.name: backstage-reader
type: kubernetes.io/service-account-token
